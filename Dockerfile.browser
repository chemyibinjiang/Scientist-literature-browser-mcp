ARG CHROMIUM_IMAGE=ghcr.io/flaresolverr/flaresolverr@sha256:139dfee1c6f89249c8d665d1333a42e8ec74ec0a86bc6bb1c8461e10d3a66a47
FROM ${CHROMIUM_IMAGE}

ARG PLAYWRIGHT_VERSION=1.58.0

ENV LANG=C.UTF-8 \
    LC_ALL=C.UTF-8 \
    PYTHONUTF8=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    HOME=/browser-profile/home \
    LITERATURE_BROWSER_PROFILE=/browser-profile/chromium \
    LITERATURE_BROWSER_CHROMIUM=/usr/bin/chromium

USER root

RUN apt-get -o Acquire::Retries=5 update \
    && apt-get -o Acquire::Retries=5 install -y --no-install-recommends \
        dbus-x11 \
        fonts-noto-cjk \
        openbox \
        x11-utils \
    && rm -rf /var/lib/apt/lists/*

RUN python3 -m pip install --no-cache-dir \
        "beautifulsoup4==4.14.3" \
        "pillow==12.1.1" \
        "playwright==${PLAYWRIGHT_VERSION}" \
        "pypdf==6.14.2" \
        "pypdfium2==4.30.0"

RUN useradd --uid 10001 --create-home --shell /usr/sbin/nologin browser \
    && mkdir -p /app /browser-profile/home /browser-profile/chromium \
    && chown -R browser:browser /app /browser-profile

COPY browser/literature_browser.py /app/literature_browser.py
COPY browser/browser_launcher.py /app/browser_launcher.py
COPY src/scientist_literature_mcp/publisher_policy.py /app/publisher_policy.py
COPY browser/literature_headed_entrypoint.sh \
    /usr/local/bin/literature-headed-entrypoint

RUN chmod 0755 /usr/local/bin/literature-headed-entrypoint

USER browser
WORKDIR /app

EXPOSE 9020

ENTRYPOINT ["/usr/bin/dumb-init", "--"]
CMD ["python3", "/app/browser_launcher.py"]
